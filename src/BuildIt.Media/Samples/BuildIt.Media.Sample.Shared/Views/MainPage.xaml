<Page
    x:Class="BuildIt.Media.Sample.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BuildIt.Media.Sample"
    xmlns:media="using:BuildIt.Media.Uno.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="Hello, world!" Margin="20" FontSize="30" />
            <TextBlock Text="{Binding Introduction}" Margin="20" FontSize="20" />
            <TextBlock Text="{Binding ResourcedIntroduction}" Margin="20" FontSize="20" />
            <Button Content="Toggle Localization" Command="{Binding ToggleLocalizationCommand}"/>
            <Button Content="Second" Command="{Binding GoSecondCommand}"/>

            <media:CameraPreview Height="200" Width="200" x:Name="preview"/>
            <Button Content="Start" Click="StartPreviewClick"/>
        </StackPanel>
    </Grid>
</Page>
